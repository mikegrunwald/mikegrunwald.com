node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# big files (served from R2, uploaded via `npm run upload-assets`) — includes
# the carousel teaser encodes in static/video/teasers/, which are generated
# locally (npm run teasers) and pushed to R2, never committed.
*.mov
*.mp4
.superpowers/
